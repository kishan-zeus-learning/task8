class Rows {
    constructor(rowHeights, mountBackPos, mountFrontPos) {
        this.rowHeights = rowHeights;
        this.mountBackPos = mountBackPos;
        this.mountFrontPos = mountFrontPos;
    }
}
export {};
